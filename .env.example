# BeChatty contact form — Web3Forms access key
# Get a free key at https://web3forms.com, then copy this file to `.env` and set the value.
# While this is empty, the contact form skips the request and shows the
# "email me directly" fallback instead of silently failing.
VITE_WEB3FORMS_KEY=
